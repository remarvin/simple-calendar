{
  "name": "simple-calendar",
  "version": "1.0.3",
  "description": "A simple JavaScript calendar with no dependencies.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"build": "babel src -d dist"
  },
  "author": "Ripley Marvin",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}